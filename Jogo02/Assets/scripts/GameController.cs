using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameController : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject splash;
    [HideInInspector] public Color32 appleColor = new Color32(116, 144, 11,255), coconutColor = new Color32(109,69,43,255), pineaplleColor = new Color32(188,123,23,255), orangeColor = new Color32(255,131,13,255), pearColor = new Color32(174,185,0,255) ;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
